<div class="post-meta page-header col-xs-12">
	<div class="col-xs-9">
	  <h1>{{{user_avatar}}}  {{display_name}} <small>Autor</small></h1>
	</div>
	{{#user_url}}
	<div class="col-xs-3">
		<h1><a class="btn btn-primary btn-lg" role="button" href="{{user_url}}" target="_blank">Web</a></h1>
	</div>
	{{/user_url}}
	<div class="col-xs-12">
		<blockquote>
			<p class="col-xs-12"><small>Bio</small>
			{{#description}}
				{{description}}
			{{/description}}
			{{^description}}
				Este usuario no ha compartido ninguna información de perfil :(
			{{/description}}
			</p>
		</blockquote>
	</div>
</div>