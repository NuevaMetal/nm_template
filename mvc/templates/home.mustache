<section id="home" class="text-center">

	{{#bandas}}
		<article class="posts">
			<div class="seccion {{seccion}}">
				{{#header}}
					{{{header}}}
				{{/header}}			
				{{> home/_posts }}	
			</div>
			{{> home/_btn_mostrar_mas }}
		</article>
	{{/bandas}}

	{{#videos}}
		<article class="posts">
			<div class="seccion {{seccion}}">
				{{#header}}
					{{{header}}}
				{{/header}}			
				{{> home/_posts }}	
			</div>
			{{> home/_btn_mostrar_mas }}
		</article>
	{{/videos}}

	{{#entrevistas}}
		<article class="posts">
			<div class="seccion {{seccion}}">
				{{#header}}
					{{{header}}}
				{{/header}}			
				{{> home/_posts }}	
			</div>
			{{> home/_btn_mostrar_mas }}
		</article>
	{{/entrevistas}}
	
</section>